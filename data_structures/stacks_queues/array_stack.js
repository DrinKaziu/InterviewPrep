let stack = [];

stack.push('google');
stack.push('instagram');
stack.push('youtube');

stack.pop();

//we can also use shift/unshift but adding and removing
//from the start of an array is not efficient sice we
//have to re-index every element.
